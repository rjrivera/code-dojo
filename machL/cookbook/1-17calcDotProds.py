import numpy as np

# I need to create the dot product of two vectors

vect_A  = np.array( [ 1, 2, 3 ] )
vect_B  = np.array( [ 2, 2, 2 ] )

dot_vect    = np.dot( vect_A, vect_B )


print( "vect A: ", vect_A, "\nvect B:", vect_B, "\ndot Prod: ", dot_vect, "\n" )


