#include "clientState.h"

clientState::clientState() : destBSlot(0), sourceBSlot(0), enemyNeighborsIndex(0), curInputState(lastEnum), selectedUnit(nullptr), myC(nullptr), cursorStack(nullptr)
{ 

}

clientState::~clientState(){

}

